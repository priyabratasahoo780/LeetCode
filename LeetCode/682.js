/**
 * @param {string[]} operations
 * @return {number}
 */
var calPoints = function(operations) {
    let res = [];
    let sum = 0;
    for(let i=0; i<operations.length; i++){
        if(i === )
    }
};